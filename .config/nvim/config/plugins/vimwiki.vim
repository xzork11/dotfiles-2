let g:vimwiki_list = [{ 'path': '~/.local/share/vimWiki', 'path_html': '~/.local/share/vimWiki/html' }]

let g:vimwiki_hl_headers = 1 						" Color the headings
let g:vimwiki_hl_cb_checked = 2					" Highligh completed TODOs
let g:vimwiki_url_maxsave = 0 					" Don't compress links
